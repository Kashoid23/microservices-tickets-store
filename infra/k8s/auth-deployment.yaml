apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      containers:
        - name: auth
          image: kashoid/tickets-store-auth
          env:
            - name: JWT_SIGN
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SIGN